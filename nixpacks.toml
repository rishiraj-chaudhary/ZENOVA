# The workspace root already scripts install/build/start for both packages, so
# the previous per-directory cd chain is no longer needed.
[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install --workspaces --include-workspace-root"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
